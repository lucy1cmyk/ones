/* =========================================================
  [Firebase Adapter JS]
  - Firebase Auth / Firestore 연결 예정 파일
  - 현재 MVP에서는 실제 Firebase SDK를 사용하지 않음
  - 나중에 Firebase 설치 후 이 파일만 교체하거나 확장하면 됨

  예상 역할:
  1. 구글 로그인
  2. 현재 로그인 유저 정보 반환
  3. 점수 저장
  4. 최근 5회 점수 불러오기
  5. 하이스코어 불러오기 / 저장
========================================================= */

const FirebaseAdapter = {
  /* =======================================================
    [Firebase 01] 초기화 예정 함수
    - Firebase SDK 설치 후 firebaseConfig 연결
  ======================================================= */
  init() {
    console.log("Firebase 초기화 예정");
  },

  /* =======================================================
    [Firebase 02] 구글 로그인 예정 함수
  ======================================================= */
  async signInWithGoogle() {
    console.log("Firebase Auth 구글 로그인 연결 예정");

    return {
      uid: "test-user",
      displayName: "테스트 유저",
      email: "test@example.com"
    };
  },

  /* =======================================================
    [Firebase 03] 현재 유저 정보 반환 예정 함수
  ======================================================= */
  getCurrentUser() {
    console.log("Firebase 현재 유저 정보 반환 예정");

    return null;
  },

  /* =======================================================
    [Firebase 04] 점수 저장 예정 함수
  ======================================================= */
  async saveScore(score) {
    console.log("Firestore 점수 저장 예정:", score);

    return {
      success: true,
      score
    };
  },

  /* =======================================================
    [Firebase 05] 최근 5회 점수 불러오기 예정 함수
  ======================================================= */
  async loadRecentScores() {
    console.log("Firestore 최근 점수 불러오기 예정");

    return [];
  },

  /* =======================================================
    [Firebase 06] 하이스코어 불러오기 예정 함수
  ======================================================= */
  async loadHighScore() {
    console.log("Firestore 하이스코어 불러오기 예정");

    return 0;
  }
};